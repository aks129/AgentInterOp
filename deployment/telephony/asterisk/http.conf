; Asterisk HTTP Server Configuration
; Required for ARI WebSocket connections

[general]
enabled = yes
bindaddr = 127.0.0.1
; Only bind to localhost for security
bindport = 8088
prefix =

[post_mappings]
; No POST mappings needed for ARI
